## Architecture of file structures

### Architecture of `src` folder

- `app`
  - the main entry point of the engine - the application
- `plugins`
  - place of plugins that can be plugged into the app
- `core`
  - code that serves the `app` and `plugins`
